import { EpisodesDirective } from './episodes.directive';

describe('EpisodesDirective', () => {
  it('should create an instance', () => {
    const directive = new EpisodesDirective();
    expect(directive).toBeTruthy();
  });
});
